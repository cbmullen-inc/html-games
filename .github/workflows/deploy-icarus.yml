name: Deploy Flappy Icarus

on:
  push:
    branches: [main]
    paths:
      - 'flappy-icarus/*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          mkdir -p _site
          cp "flappy-icarus/flappy-icarus.html" _site/flappy-icarus.html
          cp "flappy-icarus/flappy-icarus-screenshot.png" _site/flappy-icarus-screenshot.png

      - name: Deploy to gh-pages root
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          destination_dir: .
          keep_files: true